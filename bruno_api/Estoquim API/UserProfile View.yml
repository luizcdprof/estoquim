info:
  name: UserProfile View
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}/userprofile/view/"
  headers:
    - name: x-requested-with
      value: XMLHttpRequest
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
