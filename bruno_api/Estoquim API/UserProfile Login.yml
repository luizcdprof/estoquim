info:
  name: UserProfile Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/userprofile/login/"
  body:
    type: json
    data: |-
      {
        "username": "admin",
        "password": "3jenny6"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
